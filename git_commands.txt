1. git init
2. git add .
3. git commit -m "message"
4. git remote add origin <remote-url>

Example:
git remote add origin
https://github.com/GrishmaReddy159/first.git

5. git push origin main
6. you can check in remote repository

-------------------------------------------------------
If you can add any files in local repository or you an change the existing files in local repo

1. git status
2. git add .
3. git commit -m "message"
4. git push origin main
